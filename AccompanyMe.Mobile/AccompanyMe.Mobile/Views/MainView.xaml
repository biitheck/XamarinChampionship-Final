<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps" xmlns:tkmap="clr-namespace:TK.CustomMap;assembly=TK.CustomMap" x:Class="AccompanyMe.Mobile.Views.MainView" Title="Eventos">
    <!--<AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
            <tkmap:TKCustomMap x:Name="myCustomMap" IsShowingUser="true" MapType="Street" MapCenter="{Binding }" />
                       <maps:Map x:Name="mainMap" IsShowingUser="true" MapType="Street" />
        </StackLayout>
        <StackLayout IsVisible="{Binding IsBusy}" Padding="12" AbsoluteLayout.LayoutFlags="PositionProportional" AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1">
            <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" Color="Black" />
            <Label Text="Cargando..." HorizontalOptions="Center" TextColor="White" />
        </StackLayout>
    </AbsoluteLayout>-->

</ContentPage>